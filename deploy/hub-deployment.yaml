apiVersion: apps/v1
kind: Deployment
metadata:
  name: ark-hub
  namespace: ark-system
  labels:
    app: ark-hub
    component: control-plane
spec:
  replicas: 1
  selector:
    matchLabels:
      app: ark-hub
  template:
    metadata:
      labels:
        app: ark-hub
        component: control-plane
    spec:
      serviceAccountName: ark-hub-sa
      containers:
      - name: ark-hub
        image: ark-hub:latest  # 替换为实际的镜像地址，如 registry.example.com/ark-hub:v1.0.0
        imagePullPolicy: IfNotPresent
        args:
          - "--ws-listen"
          - "0.0.0.0:8080"
          - "--http-listen"
          - "0.0.0.0:8081"
          - "--enable-k8s-controller"
          # 默认不添加 --allow-execute，保持 dry-run
          # - "--allow-execute"
        ports:
        - name: websocket
          containerPort: 8080
          protocol: TCP
        - name: http-api
          containerPort: 8081
          protocol: TCP
        resources:
          requests:
            memory: "256Mi"
            cpu: "100m"
          limits:
            memory: "512Mi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /api/v1/ps
            port: 8081
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /api/v1/ps
            port: 8081
          initialDelaySeconds: 10
          periodSeconds: 5
        env:
        - name: RUST_LOG
          value: "info"
---
apiVersion: v1
kind: Service
metadata:
  name: ark-hub
  namespace: ark-system
  labels:
    app: ark-hub
    component: control-plane
spec:
  type: ClusterIP
  ports:
  - name: websocket
    port: 8080
    targetPort: 8080
    protocol: TCP
  - name: http-api
    port: 8081
    targetPort: 8081
    protocol: TCP
  selector:
    app: ark-hub
