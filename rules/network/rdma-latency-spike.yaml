id: network.rdma.latency_spike.v1
name: "RDMA 延迟尖峰"
scene: "network.rdma.latency_spike"
priority: 86
reason_codes:
  - "RDMA_LATENCY_SPIKE"
conditions:
  - type: "event"
    event_type: "error.net"
    value_pattern: "rdma_latency|rdma_timeout|rdma"
  - type: "event"
    event_type: "transport.drop"
    value_pattern: "retransmit|drop"
root_cause_pattern:
  primary: "RDMA 网络抖动导致训练通信退化"
  secondary:
    - "交换机拥塞"
    - "链路抖动"
solution_steps:
  - step: 1
    action: "检查 RDMA 错误计数"
    command: "ethtool -S <iface>"
    manual: false
  - step: 2
    action: "切换拥塞控制或限速"
    command: null
    manual: true
related_evidences:
  - "error.net"
  - "transport.drop"
applicability:
  min_confidence: 0.8
  required_events:
    - "error.net"
